apiVersion: v1
kind: Service
metadata:
  name: akakraft-minio
  namespace: akakraft-prod
spec:
  selector:
    app: akakraft-minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
  clusterIP: None
