apiVersion: v1
kind: Service
metadata:
  name: akakraft-postgres
  namespace: akakraft
spec:
  selector:
    app: akakraft-postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # Headless Service – direkte DNS-Auflösung für StatefulSet
